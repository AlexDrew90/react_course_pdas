let ProfileCard = ({title, handle}) => {
  // const title = props.title
  // const handle = props.handle
  return <div>
          <p>The assistant is called {title} and the handle is {handle}.</p>
        </div>
}

export default ProfileCard;
